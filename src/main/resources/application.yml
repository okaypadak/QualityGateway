server:
  port: ${PORT:8080}

spring:
  main:
    web-application-type: reactive
  cloud:
    gateway:
      default-filters:
        - JwtAuthFilter
      routes:
        - id: urun-servisi
          uri: qualitytest
          predicates:
            - Path=/urun/**
          filters:
            - name: JwtAuthFilter